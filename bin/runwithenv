#!/bin/bash
# 1.01 psc Tue Oct 07 16:31:14 BST 2014 - Now directs to 'pclog' directory for logs.
# 1.00 arb Fri Oct  3 16:01:12 BST 2014
#
# This script simply sources the ingest environment variables before
# running the command given as parameters.
#
# It is intended to be used with the dosemu "unix" program so that
# DOS batch files can run unix programs. DOS doesn't pass the env vars
# through via unix so we need to set them ourself.
# For example dopcal.bat could be "unix /opt/DSSingest/bin/runwithenv dopcal"
#
. /opt/DSSingest/hrptenv.bashrc

HLOGDIR=$HRPT/pcexport/pclog
export HLOGDIR

"$@"
